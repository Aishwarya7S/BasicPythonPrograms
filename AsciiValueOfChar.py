ch = int(input("Enter the character: "))
print("ASCII value of", ch, "is", ord(ch))

#ord() : It converts the given string of length one, returns an integer representing the Unicode code point of the character. 
